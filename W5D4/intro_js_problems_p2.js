function titleize(names, cb) {
  let titleized = names.map(name => name.toUpperCase)
  cb(titleized)
}
//not finished

function Elephant {
  this.name = name;
  this.height = height;
  this.tricks = tricks;
}

Elephant.prototype.trumpet {
  
}
